<header class="navbar navbar-default navbar-fixed-top" role="navigation">
  <div class="navbar-inner">
    <div class="container">
  		<div class="navbar-header">
  			<%= link_to(session[:unit_name].present? ? session[:unit_name] : t("corporation"), root_path, class: "navbar-brand") %>
  		</div>

  		<% if user_signed_in? %>
        <ul class="nav navbar-nav">  			
          <li><%= link_to(t("menu.home"), '#') %></li>
          <li class="dropdown">
            <a href="#" class="dropdown-toggle" data-toggle="dropdown">Cadastros<span class="caret"></span></a>
            <ul class="dropdown-menu" role="menu">
              <li><%= link_to(t("menu.unit"), '#') %></li>
              <li><%= link_to(t("menu.employee"), '#') %></li>
              <li><%= link_to(t("menu.client"), '#') %></li>
              <li><%= link_to(t("menu.taxpayer"), '#') %></li>
              <li class="divider"></li>
              <li><%= link_to(t("menu.myuser"), '#') %></li>
              <li><%= link_to(t('menu.user'), '#') %></li>
            </ul>
          </li>
          <li class="dropdown">
            <a href="#" class="dropdown-toggle" data-toggle="dropdown">Atendimento<span class="caret"></span></a>
            <ul class="dropdown-menu" role="menu">
              <li><%= link_to(t("menu.negotiation"), '#') %></li>
            </ul>
          </li>
        </ul>

      	<ul class="nav navbar-nav navbar-right">
          <li>
            <a>
              <strong><%= current_user.email %></strong>
            </a>
          </li>
      	  <li class='active'><%= link_to t("logout", default: "Logout"), destroy_user_session_path, method: :delete %></li>
        </ul>
  		<% end %>
    </div>
  </div>
</header>
