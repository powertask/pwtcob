<%= simple_form_for(@ticket) do |f| %>
	<div class='section'>
		
    <%= f.input :amount, :label => t('proposal.name'), input_html:{:class => 'form-control'} %>

		<div class="actions">
			<div class='buttonbar'>
				<%= f.button :submit, t('menu.save'), class: 'btn-primary' %>
				<%= 'ou' %>
				<%= link_to t('menu.cancel'), clients_path %>
			</div>
		</div>
	</div>
<% end %>
